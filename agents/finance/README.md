# Finance Agent

Phase 1, Week 3 (v1: reads a manually maintained expense file, answers questions, produces a short review — no live bank integration). Matures into a real review cadence in Phase 3, alongside the advisory-only Investment Agent.

Model: `premium` tier, falling back to `premium-fallback` then `mid` — see `docs/MODEL_ROUTING.md`. Real money, so it doesn't get a cheap/free tier even in v1.

Not started.
