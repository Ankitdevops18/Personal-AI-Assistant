# Daily News Agent

Phase 1, Week 2. Simplest agent in the roster (fetch → summarize → write note) — good first test of the daily-automation pipeline before Finance relies on it.

Model: `cheap` tier (routes through the free-provider waterfall automatically) — see `docs/MODEL_ROUTING.md`. This is exactly the high-volume/low-stakes work that tier exists for.

Not started.
