# Memory — Postgres + pgvector

Phase 2. Structured state (goals, habits, tasks, expenses, learning progress, agent events) plus semantic search over the Obsidian vault, in one Postgres instance via the pgvector extension — deliberately not a separate vector DB (see the master plan's tech-stack analysis, section 6.3, for why).

Not started.
